class Destination {
  String id;
  double amount;
  String currency;
  String description;
  String reference;

  Destination(
      this.id, this.reference, this.amount, this.currency, this.description);
}
