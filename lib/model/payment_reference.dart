class Reference {
  String acquirer;
  String gateway;
  String payment;
  String track;
  String transaction;
  String order;
  String gosellID;

  Reference(this.acquirer, this.gateway, this.gosellID, this.order,
      this.payment, this.track, this.transaction);
}
